import abc


class AbstractResolver(abc.ABC):
    """
    A resolver takes raw information in a configuration
    file and attempts to extract the information that is given
    into meaningful infrastructure model code
    """
    pass
